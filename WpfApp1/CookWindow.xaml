<Window x:Class="WpfApp1.CookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1" xmlns:viewmodels="clr-namespace:WpfApp1.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="CookWindow" Height="450" Width="800">
    <Grid Background="#C2CAD0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox
            x:Name="ListOfOrders"
            Grid.Row="0"
            Grid.Column ="0"
            Grid.RowSpan="3"
            Margin="15 10 15 20"
            ItemsSource="{Binding CheckOrder, UpdateSourceTrigger=PropertyChanged}"
            SelectedItem="{Binding SelectedOrder}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <Border
                            BorderBrush="#EE4C7C">
                        </Border>
                        <TextBlock
                            x:Name="IdOfOreder"
                            Grid.Column="0"
                            Grid.Row="0"
                            Text="{Binding StringFormat=Номер заказа: {0}, Path =Id, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0 0 4 0">
                        </TextBlock>
                        <TextBlock
                            x:Name="DishInOreder"
                            Grid.Column="2"
                            Grid.Row="0"
                            Text="{Binding StringFormat=Статус: {0}, Path = StatusNavigation.Title, UpdateSourceTrigger=PropertyChanged}"
                            FontSize="12"
                            FontFamily="Cousine">
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox
            x:Name="ListOfCompleteOrders"
            Grid.Row="3"
            Grid.Column ="0"
            Grid.RowSpan="3"
            Margin="15 10 15 20"
            ItemsSource="{Binding CheckCompleteOrder, UpdateSourceTrigger=PropertyChanged}"
            SelectedItem="{Binding SelectedOrder}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <Border
                            BorderBrush="#EE4C7C">
                        </Border>
                        <TextBlock
                            x:Name="IdOfOreder"
                            Grid.Column="0"
                            Grid.Row="0"
                            Text="{Binding StringFormat=Номер заказа: {0}, Path =Id, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0 0 4 0">
                        </TextBlock>
                        <TextBlock
                            x:Name="DishInOreder"
                            Grid.Column="2"
                            Grid.Row="0"
                            Text="{Binding StringFormat=Статус: {0}, Path = StatusNavigation.Title, UpdateSourceTrigger=PropertyChanged}"
                            FontSize="12"
                            FontFamily="Cousine">
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock
           Name="TimeOfOrder"
           Grid.Column="1"
           Grid.Row="0"
           Text="{Binding OrderDetailes.Order.Time}"
           Margin="15 10 5 10" 
           FontSize="12"
           FontFamily="Cousine"
           Background="#c2cada"
            />
        <TextBlock
           Name="GeneralTime"
           Grid.Column="1"
           Grid.Row="1"
           Text="{Binding StringFormat =Общее время приготовления {0},Path = GenaralTime}"
           Margin="15 0 0 0"
           FontSize="14"
           FontFamily="Cousine"
           VerticalAlignment="Center"
           Background="#c2cada"
            />
        <ListBox
            Grid.Column ="1"
            Grid.Row="3"
            Grid.RowSpan="2"
            Margin="15 10 15 20"
            Background="#c2cada"
            ItemsSource="{Binding DishesInOrders}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <TextBlock
                            Name="TitleOfDish"
                            Grid.Column="0"
                            Grid.Row="0"
                            Text="{Binding StringFormat=Наименование {0},Path = Dish.Title}"
                            Margin="0 0 4 0"
                            FontSize="14"
                            FontFamily="Cousine"
                            />
                        <TextBlock
                            Name="TimeOfDish"
                            Grid.Column="1"
                            Grid.Row="0"
                            Text="{Binding StringFormat=Время {0} минут ,Path = Dish.Time}"
                            FontSize="14"
                            FontFamily="Cousine"
                            />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button
            Name="GetButton"
            Grid.Column="1"
            Grid.Row="5"
            Width="170"
            Height="50"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Content="Принять"
            FontSize="14"
            FontFamily="Cousine"
            Command="{Binding ChangeStatus}"
            />
        <Button
            Name="FinishButton"
            Grid.Column="1"
            Grid.Row="5"
            HorizontalAlignment="Right"
            Width="170"
            Height="50"
            VerticalAlignment="Center"
            Content="Выполнен"
            FontSize="14"
            FontFamily="Cousine"
            Command="{Binding FinishButton}"
            />
    </Grid>
</Window>